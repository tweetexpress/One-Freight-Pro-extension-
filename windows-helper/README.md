# ONE Freight Pro Windows Helper

Run `install-protocol.ps1` once from this folder. It registers the local `dathelper:` protocol so the browser extension can hand email requests to Outlook.

Re-run it any time this folder is moved.
